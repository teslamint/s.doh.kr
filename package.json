{
  "name": "siliconbeest-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r --if-present build",
    "deploy": "pnpm -r --if-present deploy",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "packageManager": "pnpm@10.9.0"
}
